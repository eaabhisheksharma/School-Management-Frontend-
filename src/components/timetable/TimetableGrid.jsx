import React from 'react';
const TimetableGrid = ({ data }) => (
  <div style={{ padding: 16 }}>
    <p style={{ color: '#64748b' }}>Timetable grid coming soon.</p>
  </div>
);
export default TimetableGrid;
